import { BindhostDirective } from './bindhost.directive';

describe('BindhostDirective', () => {
  it('should create an instance', () => {
    const directive = new BindhostDirective();
    expect(directive).toBeTruthy();
  });
});
